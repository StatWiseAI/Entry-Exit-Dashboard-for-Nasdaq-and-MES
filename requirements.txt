# NQ/ES AI Trade Decision System
# ================================
# pip install -r requirements.txt

# ── Core strategy engine ──────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0

# ── Streamlit web app ─────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── AI agent — Gemini (free default) ─────────────────────────────────────────
# Get a free key at: https://aistudio.google.com/app/apikey
# Add to Streamlit secrets: GEMINI_API_KEY = "AIza..."
google-genai>=1.0.0

# ── AI agent — Anthropic Claude (optional upgrade) ───────────────────────────
# Add to Streamlit secrets: ANTHROPIC_API_KEY = "sk-ant-..."
# The system switches automatically when this key is present.
anthropic>=0.30.0

# ── Live data connectors ──────────────────────────────────────────────────────
requests>=2.31.0

# ── Optional live data sources (uncomment as you connect them) ────────────────
# yfinance>=0.2.40         # VIX real-time
# websocket-client>=1.7.0  # Tradovate WebSocket
# flask>=3.0.0             # TradingView webhook receiver
# newsapi-python>=0.2.7    # NewsAPI
